const Author = require('./author-model');
const { handleErr } = require('../util/helpers');

module.exports = {
  get: (req, res) => {},
  getMany: (req, res) => {},
  add: (req, res) => {},
  update: (req, res) => {},
  remove: (req, res) => {}
}
